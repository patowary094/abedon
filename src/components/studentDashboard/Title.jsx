import styles from './title.module.css'

// invert an array

function Title() {
    return (
        <div className={styles.title}>
            <h1>Dashboard</h1>
        </div>
    )
}

export default Title
